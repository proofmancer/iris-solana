.anchor/
.DS_Store
target/
**/*.rs.bk
node_modules/
test-ledger/
.idea/
.vscode/
**/dist/
**/build/
*.log

# Keypairs — never commit
*.json.key
keys/
.env
.env.*
!.env.example

# Agent / superstack tooling context (local-only).
.agents/
.superstack/
skills-lock.json
*-help.txt

# Local lockfile reflects a non-working local toolchain attempt; regen with  against a known-good Solana setup.
Cargo.lock
